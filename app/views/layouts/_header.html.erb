<!DOCTYPE html>
<html lang="ru">
<head>
    <link rel="stylesheet" href="https://unpkg.com/swiper/swiper-bundle.min.css" />
    <script src="https://unpkg.com/swiper/swiper-bundle.min.js"></script>
    <%= stylesheet_link_tag 'header', media: 'all', 'data-turbolinks-track': 'reload' %>
    <% if controller_name == 'main' && action_name == 'index' %>
    <%= stylesheet_link_tag 'index', media: 'all', 'data-turbolinks-track': 'reload' %>
  <% elsif controller_name == 'blogs' && action_name == 'index' %>
    <%= stylesheet_link_tag 'blogs', media: 'all', 'data-turbolinks-track': 'reload' %>
  <% elsif controller_name == 'admin' && action_name == 'index' %>
    <%= stylesheet_link_tag 'adminka', media: 'all', 'data-turbolinks-track': 'reload' %>
  <% elsif controller_name == 'validate_admin' && action_name == 'index' %>
    <%= stylesheet_link_tag 'proverka', media: 'all', 'data-turbolinks-track': 'reload' %>
  <% end %>



    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Мистер ДВОРИК</title>
    <%= favicon_link_tag asset_path('ico.jpg') %>
</head>
<body>
<header>
    <div class="header-content">
        <div class="contacts">
            <p>Телефон: +7 (123) 456-78-90</p>
            <p>Адрес: г. Омск, ул. Примерная, д. 1</p>
        </div>
        <div class="buttons" id="navLinks">
            <button><%= link_to 'наш блог', blogs_path  %></button>
            <button><%= link_to 'главная', root_path  %></button>
            <%= link_to 'Связаться через звонок', root_path(anchor: 'appointment') %>
            <button><a href='https://wa.me/79514142979'>Написать в WhatsApp</a></button>  
            <% if session[:admin] %>
                <%= button_to "Выход из сесии админа", destroy_admin_session_path, method: :post, class: 'delete-button' %>
                <button><%= link_to 'админ-панель', admin_path  %></button>
            <% end %>
        </div>
        <div class="burger-menu" id="burgerMenu">
            <div class="burger-icon" onclick="toggleMenu()">
                <div class="bar"></div>
                <div class="bar"></div>
                <div class="bar"></div>
            </div>
        </div>
    </div>
</header>

<script>
    function toggleMenu() {
        var navLinks = document.getElementById('navLinks');
        navLinks.classList.toggle('active');
    }
</script>

